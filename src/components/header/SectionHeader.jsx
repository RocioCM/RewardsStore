function SectionHeader({title}) {
	//Electronics header: background image is set in css.
	return (
		<section className='section-header'>
			<h1 className='header-title'>{title}</h1>
		</section>
	);
}

export default SectionHeader;
